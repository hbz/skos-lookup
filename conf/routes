# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)
POST 	/init						controllers.SkosToElasticsearch.init(dataDirectory:String)
GET 	/example					controllers.SkosToElasticsearch.autocompleteExample
GET		/autocomplete				controllers.SkosToElasticsearch.autocomplete(q:String,lang:String)
GET		/search					    controllers.SkosToElasticsearch.search(q:String)